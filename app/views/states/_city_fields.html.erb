<fieldset>
  <div class="fields">
    <ul class="unstyled">
      <li class="well control-group">
        <%= f.label :name, 'City Name', :class=>"control-label" %>
        <div class="controls">
        <%= f.text_field :name, :class => "city_name_field" %>
        <%= link_to_remove_fields 'Remove', f %>
        </div>
      </li>
      <%= f.fields_for :areas do |builder| %>
          <%= render 'area_fields', :f => builder %>
      <% end %>
      <li class="form-actions">
        <%= link_to_add_fields "Add Area", f, :areas %>
      </li>
    </ul>
  </div>
</fieldset>
